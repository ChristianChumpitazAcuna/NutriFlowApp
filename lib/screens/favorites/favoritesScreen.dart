import 'package:flutter/material.dart';

class FavoritesScreem extends StatelessWidget {
  const FavoritesScreem({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        color: Colors.blue,
        child: const Center(
          child: Text('Favorites Screen'),
        ));
  }
}
